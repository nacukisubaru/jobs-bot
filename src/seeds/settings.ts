import mongoose from 'mongoose';

const MONGO_URI = process.env.MONGO_URI ?? 'mongodb://localhost:27017/your_database';

const settingSchema = new mongoose.Schema(
  {
    key: { type: String, required: true, unique: true },
    value: { type: mongoose.Schema.Types.Mixed, required: true },
  },
  { timestamps: true },
);

const SettingModel = mongoose.model('Setting', settingSchema);

async function upsert(key: string, value: unknown): Promise<void> {
  await SettingModel.updateOne({ key }, { $set: { value } }, { upsert: true });
  console.log(`✓ upserted key="${key}"`);
}

async function seed(): Promise<void> {
  await mongoose.connect(MONGO_URI);

  // ─── 1. resume ───────────────────────────────────────────────────────────────

  await upsert('resume', {
    resumeExamples: [
      {
        company: 'SUPA',
        experience: `
2 года
Fullstack-разработчик
Февраль 2024 — Январь 2026 (2 года)

Генерация QR-кодов
Разработал интерактивный модуль с динамическими формами, цветом и логотипами.
Интегрировал qrcode.react, доработал SVG вручную: устранил микрозазоры (feMorphology), 
сгладил aliasing масштабированием по центру, уникализировал clipPath/filter id, реализовал собственную отрисовку бейджа.
Результат: стабильный рендер на canvas и корректная работа Undo/Redo.

Стек: React, SVG, Canvas, Svelte, qr.code.react, react-hook-forms

Скачивание отдельных слайдов / предпросмотр
Создал функционал выбора и скачивания отдельных слайдов с множественным select и мини-превью внутри dropdown.

Разработал MiniPlayer на базе Player (рендер холста редактора) с вычисляемым масштабированием холста через zoom.

Проработал оптимизацию:
lazy-loading через IntersectionObserver,
исключение аудио/видео/скрытых объектов, отключение анимаций, очистка shared-данных, оптимизация структуры объектов,
memoized selectors (reselect) для предотвращения лишних пересчётов.

Результат: плавный предпросмотр даже при большом количестве слайдов, интерфейс стабилен на слабых устройствах.

Стек: React, Canvas, Redux, reselect

Разработал форму обратной связи с выбором креативов, вложениями и клиентской валидацией.
Реализовал автоматический сбор технического контекста пользователя (ОС, браузер, версия приложения, ID креатива) 
для ускорения обработки обращений.
Внедрил Preloader, многоуровневую валидацию и отправку тикетов через Nodemailer, 
улучшив UX и повысив надежность обработки заявок.

Результат: сокращено время обработки тикетов, меньше уточняющих запросов,
 стабильная доставка обращений с полным контекстом.

Стек: Nest.js, Express, nodemailer, React, react-hook-forms

AI-модуль генерации изображений (React, helpersQueue, AbortController, Canvas, Sharp, AWS)
Разработал визуальный интерфейс с выбором модели генерации (ChatGPT + Fabula) и загрузкой исходного изображения.
Реализовал генерацию похожих изображений с автоматическим промптом и передачей данных в API.
Спроектировал асинхронную очередь задач (helpersQueue) с контролем статусов, 
защитой от гонок и отменой через AbortController.
Оптимизировал изображения через Sharp и реализовал загрузку на AWS.
Результат: Генерация картинок для последующего использования на Canvas, 
безопасная генерация без зависаний; разработал масштабируемый и надежный AI-модуль 
генерации изображений, повысивший востребованность продукта среди пользователей.

Стек: ChatGPT, Fabula AI, AbortController, React, Canvas, queue, sharp, aws

Достижения:

Перенёс загрузку изображений с клиента на бэкенд: теперь клиент отправляет один запрос, 
а сервер скачивает, ресайзит и загружает в S3 напрямую. Это снизило нагрузку на клиент, 
уменьшило количество сетевых запросов и ускорило процесс загрузки.

Оптимизировал экспорт слайдов (FFmpeg)
Переработан серверный пайплайн: генерация видео-фрагментов из скриншотов с аудиодорожкой.
Результат: Генерируется отдельно каждый слайд, а не видео целиком, ускорена сборка видео, снижена нагрузка на сервер.`,
      },
      {
        company: 'Uniqtripsoft',
        experience: `
2 года
Fullstack-разработчик
Январь 2022 — Декабрь 2023 (2 года)

SlotBy - система бронирования столиков в ресторанах
Разработал:
интерактивную карту со столиками, индикаторы нагруженности каждого столика;
таймлайн с автопереключением и автопрокруткой по времени;
форму бронирования с учетом конфликта, если столик уже был занят, интерфейс 
это удобно подсвечивает ввиде ошибки и дает возможность в ручную разрешить этот конфликт выбрав нужный столик;

Достижения:
Заложил fsd архитектуру, что сделало проект масштабируемым;
Применил websoket для отображения состояния столиков в real-time;

Стек: React, redux, tailwind, scss, python

visit-sochi.ru - бронирование отелей и экскурсий

Реализовал библиотеку для работы с bitrix api:
миграции и seeds;
работа с crud операциями в bitrix;
d7;

Реализовал личный кабинет партнера, в котором партнер мог создавать отели, 
типы размещения, экскурсии, места расположения экскурсий и прочее

Подключил яндекс карты для отображения отелей и экскурсий на картах при бронировании.
Оптимизировал рендер точек на картах, так точек было много

Реализовал регистрацию и авторизацию через oauth2 и jwt

Стек: php, bitrix, react, redux

Евромед - сервис для бронирования записи к врачу
Реализовал:
апи для бронирования;
админ панель для заполнения специализаций, врачей, клиник;

Стек: php, bitrix, react, redux`,
      },
      {
        company: 'itsheff.kz',
        experience: `
1 год
Fullstack-разработчик
Январь 2021 — Декабрь 2021 (1 год)

CRM система и облачные решения 1С

Разрабатал систему для управления сотрудниками внутри CRM
автоматизировал процессы приема, увольнения и рабочего расписания сотрудников, 
визуализировал смены, отпуска и недоступность через FullCalendar, обеспечивал масштабируемое 
обновление графиков на месяц и год с WebSocket и RabbitMQ, интегрировал IP-телефонию (Asterisk)
внедрил realtime обновление заявок клиентов и оптимизацию обработки высоконагруженных расписаций.

Стек: React, node.js, php, redux`,
      },
    ],
    experiencePeriods: [
      { start: { month: '02', year: '2024' }, end: { month: '01', year: '2026' } },
      { start: { month: '01', year: '2022' }, end: { month: '12', year: '2023' } },
      { start: { month: '01', year: '2021' }, end: { month: '12', year: '2021' } },
    ],
  });

  // ─── 2. career-preferences ───────────────────────────────────────────────────

  await upsert('career-preferences', {
    specializations: [
      { id: 1, name: 'Frontend React разработчик' },
      { id: 2, name: 'Fullstack-разработчик (React.js, Node.js)' },
      { id: 3, name: 'Битрикс разработчик' },
    ],
    contacts: 'tg: @krestinskiy99, email: nacukisubaru99@yandex.ru, телефон: +79950567738',
    fio: 'Александр Крестинский',
    salary: 150000,
    aboutMe: `Разрабатываю высокопроизводительные интерфейсы с продуманным UX: оптимизация рендеринга и
     памяти, виртуализация списков и карточек, drag & select, плавный скролл, борьба с лагами и пиковыми
      нагрузками, адаптивный UX для мобильных и десктопов.
    Работал в проектах с жёсткими стандартами качества кода (ESLint с Airbnb Style Guide), 
    реализовывал многоязычность через i18next, использовал Docker для локальной разработки 
    и воспроизводимого окружения проектов.

Мои навыки:
React.js, React Hooks, Svelte, React-bootstrap,
Redux, Redux Toolkit, Mobx, Redux undo,
SCSS, Tailwind, MaterialUI, GravityUI,
Typescript, Vite,
Nest.js, Node.js, Express,
Mongoose, Mongodb, Postgressql,
Aws s3, JWT, Sharp,
Canvas, Axios, Gulp
Air-bnb-styleguide,
Eslint,
React-hook-forms,
Final-form,
Github, Gitlab, git, CI/CD
Rest, abort controller, Websockets
Docker, Postman
Kanban,
FSD,
i18next`,
  });

  console.log('\n✅ Seed completed successfully.');
  await mongoose.disconnect();
}

seed().catch((err) => {
  console.error('❌ Seed failed:', err);
  process.exit(1);
});
